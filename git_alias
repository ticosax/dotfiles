[alias]
    co = checkout
    st = status
    rbi = "!git rebase -i $(git merge-base $(git rev-parse --abbrev-ref HEAD) master)"
    cm = "!for name in $(git branch --merged | grep -v master | grep -v '*'); do git branch -d $name; done"
